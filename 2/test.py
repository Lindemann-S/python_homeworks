# from mpmath import *
# mp.dps = 100
# print(mp)


def main():
    # m = 1e-28
    # my_gamma = 1e-3
    # h = 1e-27
    # big_e = 1e-3
    # print("value_without_sqrt:", end=" ")
    # print((mpf(m) * mpf(my_gamma))**2 + 2 * mpf(h)**2 * mpf(m) * mpf(big_e))
    # print("value_with_sqrt:", end=" ")
    # print(((mpf(m) * mpf(my_gamma))**2 + 2 * mpf(h)**2 * mpf(m) * mpf(big_e))**0.5)
    # print("value_with_sqrt - m * gamma:", end=" ")
    # print(((mpf(m) * mpf(my_gamma))**2 + 2 * mpf(h)**2 * mpf(m) * mpf(big_e))**0.5 - mpf(m) * mpf(my_gamma))
    # print("m * gamma:", end=" ")
    # print(mpf(m) * mpf(my_gamma))
    # print("result:", end=" ")
    # print((((mpf(m) * mpf(my_gamma))**2 + 2 * mpf(h)**2 * mpf(m) * mpf(big_e))**0.5 - mpf(m) * mpf(my_gamma)) / mpf(h)**2)
    a = (1, 2, 3)
    b = a
    a = (1, 2)
    print(b)


if __name__ == "__main__":
    main()
